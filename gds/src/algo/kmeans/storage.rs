//! K-Means storage runtime.
//
// This is a placeholder to match the standard algorithm module layout.
// The current crate runs KMeans through the procedure facade which already
// materializes feature vectors from node properties.

#[derive(Debug, Default, Clone)]
pub struct KMeansStorageRuntime;
