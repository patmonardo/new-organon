// Boundary-schema facade.
// The kernel FormProcessor (Absolute Form) is implemented in Rust under `gds/`.
// This TS module intentionally only re-exports the GDSL schema surface.
export * from '@organon/gdsl/schema';
